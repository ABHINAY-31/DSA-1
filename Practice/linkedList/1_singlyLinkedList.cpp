#include <bits/stdc++.h>
using namespace std;

struct node{
    int data;
    node* next;
    node(int val){
        this->data = val;
        this->next = nullptr;
    }
};

void createList(node* &head,int data){
    node* newnode = new node(data);
    if(head == nullptr){
        
    }
}


int main(){
    node* head = nullptr;

}